@{
    Layout = "~/_Layout.cshtml";
    Page.Title = "Welcome to MegaDesk";

    var db = Database.Open("MegaDesk");
    var selectCommand = "SELECT dq.DateAdded, dq.CustomerName, d.Height, d.Width, d.NumberOfDrawers, s.Material, r.RushOrderOption, dq.FinalQuote FROM DeskQuote dq INNER JOIN Desk d ON dq.DeskId = d.DeskId INNER JOIN SurfaceMaterial s ON d.SurfaceMaterial = s.SurfaceMaterialId INNER JOIN RushOrderRates r ON dq.RushOption = r.RushOrderId ";
    var searchTerm = "";

    //if (!Request.QueryString["searchName"].IsEmpty())
    //{
    //  selectCommand = "SELECT * FROM QuoteTable WHERE CustomerName LIKE @0";
    //searchTerm = "%" + Request["searchName"] + "%";
    //}

    var selectedData = db.Query(selectCommand, searchTerm);
    var grid = new WebGrid(source: selectedData, defaultSort: "DateAdded", rowsPerPage: 5);
}
<div id="form" style="background-image:url(http://www.woodntap.com/packages/theme_wnt/themes/woodntap/css/images/background-pattern.jpg);width:100%;height:100%">
    <h2>List Quotes</h2>
    <form method="get" >
        <div id="search">
            <label for="searchName">Search by Customer Name:</label>
            <input type="text" name="searchName" value="@Request.QueryString["searchName"]" />
            <input type="Submit" value="Search" /><br />
        </div>
    </form>
    <div>
        @grid.GetHtml(
            tableStyle: "grid",
            headerStyle: "head",
            alternatingRowStyle: "alt",
            columns: grid.Columns(
    @*            grid.Column(format: @<a href="~/EditQuote?id=@item.Id">Edit</a>),*@
                            grid.Column("DateAdded", format: (item) => string.Format("{0:MM-dd-yyyy}", item.DateAdded)),
                            grid.Column("CustomerName"),
                            grid.Column("Height"),
                            grid.Column("Width"),
                            grid.Column("NumberOfDrawers"),
                            grid.Column("Material"),
                            grid.Column("RushOrderOption"),
                            grid.Column("FinalQuote")))
   @*                         grid.Column(format: @<a href="~/DeleteQuote?id=@item.Id">Delete</a>))) *@
    </div>
    <p>
        <a href="~/AddQuote">Add a Quote</a>
    </p>
</div>